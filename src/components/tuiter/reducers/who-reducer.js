import whoJson from '../data/whos.json'; // import data from JSON file

const whoReducer = (state = whoJson) => {   // initialize the reducer's state
    return(state);                          // return the single static state
};

export default whoReducer;